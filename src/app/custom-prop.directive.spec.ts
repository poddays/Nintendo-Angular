import { CustomPropDirective } from './custom-prop.directive';

describe('CustomPropDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPropDirective();
    expect(directive).toBeTruthy();
  });
});
